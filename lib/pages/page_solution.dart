import 'package:flutter/material.dart';

class PageSolution extends StatelessWidget{

  const PageSolution({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(
        child: Text("Mate"),
      ),
    );
  }
}