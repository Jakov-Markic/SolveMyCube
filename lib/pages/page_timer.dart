import 'package:flutter/material.dart';


class PageTimer extends StatelessWidget {
  const PageTimer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text("timer"),
        ),
        
        
      );
  }
}